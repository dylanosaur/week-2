/**
 * Git: Version Control Software (VCS) and Source Code Management (SCM) tool
 * should be committing after finishing a feature / stable points
 * 
 * git commit
 * git commit -m "message"
 * 
 * git push
 * git push origin <branch>    // (branch -> master)
 * 
 * git clone <url>
 * 
 * git pull
 * git pull origin <branch>
 * 
 * git add <file>
 * git add .        // everything in current folder and 2 subfolders
 * 
 * git init         // create new repo
 * 
 */

 /**
  * git branch      // lists branches
  * git checkout <branch>
  * git checkout -b branch-name         // create and checkout branch
  * git branch -d <branch>              // delete branch
  * 
  */